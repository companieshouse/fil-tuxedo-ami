# IBM Informix license installer response file; this file can be generated by
# running java -jar we_edition.jar -r <file-path> and can be used to perform
# a non-interactive license installation with the following command:
# java -jar we_edition.jar -i silent -f <file-path>

LICENSE_ACCEPTED=TRUE
USER_INSTALL_DIR={{ informix_install_directory }}
